#include <stdio.h>
#include <stdlib.h>

int main()
{

    /**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

    int n = 0;

printf("Enter value's : %d\n", n);
   if( n > 0 )
    {
      printf("n is less\n" );
   }
    if( n < 3)
    {
      printf("n is not less\n" );
   }
   printf("value of n is : %d\n", n);

    printf("Hello world!\n");
    return 0;
}
